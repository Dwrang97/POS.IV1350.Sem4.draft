package se.kth.iv1350.sem3.integration;

import se.kth.iv1350.sem3.model.Sale;

/**
 * <code>ExternalAccountingSystem</code> is a placeholder for the actual external accounting system.
 * This class will only be used to send information about the sale when the sale is finished.
 */
public class ExternalAccountingSystem {
    /**
     * <code>sendSaleInformation</code> sends information about the sale to the external accounting system, but is not
     * implemented further then a method representing that part.
     * * @param sale is the sale object that will be sent to the external accounting system.
     */
   public void sendSaleInformation(Sale sale){

   }
}
